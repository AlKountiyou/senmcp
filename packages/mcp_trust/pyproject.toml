[project]
name = "mcp-trust"
version = "0.1.0"
description = "Trust & safety policies, provenance, and audit logging for the SenCivic MCP Stack."
requires-python = ">=3.11"
dependencies = [
  "mcp-core==0.1.0",
  "pydantic==2.11.0",
]

